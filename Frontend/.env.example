# API base URL used in production builds
# For dev, we proxy to /api/v1 by default via axios baseURL fallback.
VITE_API_BASE_URL=https://your-api.example.com/api/v1

# Optional: override default app title
VITE_APP_NAME=ByteLearn
